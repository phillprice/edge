---
# Exclude non-shipped tooling and test data from Codacy static analysis.
exclude_paths:
  - 'backend/scripts/**'
  - 'backend/db/__fixtures__/**'
  - '.claude/**'        # Claude Code agent worktrees and memory — not production code
  - 'frontend/coverage/**'
  - 'backend/coverage/**'
